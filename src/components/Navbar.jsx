import { NavLink } from "react-router-dom";

function Navbar() {

    return (
        <div className="Navbar">
            <NavLink role="navigation" to='/'>🏠</NavLink>
        </div>
    )
}

export default Navbar;
